# Explica el flujo completo desde que un admin hace login hasta que puede agregar un plato. Menciona cada archivo que participa y qué hace cada uno.

Primero comienza en el archivo app.js el cuál ve que la ruta es /auth así que lo lleva a al archivo auth.routes en la carpeta routes en la que ve que hace el método post con el /login entonces lo lleva al archivo auth.controller en el que realiza la función login que llama a la función login que se encuentra en el archivo auth.service.js la cual usa el método findOne sobre el modelo AuthUser que se crea en el archivo auth.model.js que usa la librería mongoose de MongoDb, y con eso el auth controller luego de que verifica que el email y la contraseña coinciden, envía una respuesta con el estatus de 200 y el token en formato json al administrador.

# ¿Por qué bcrypt.compare() puede verificar la contraseña si el hash es irreversible? ¿Qué está haciendo internamente?

# Si alguien roba un token JWT válido, ¿qué puede hacer con él? ¿Qué mecanismo del sistema limita el daño? ¿Qué agregarías para hacerlo más seguro?
